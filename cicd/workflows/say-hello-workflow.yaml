apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  # generateName: hello-workflow-
  name: say-hello-wf
spec:
  arguments:
    parameters:
    - name: location
      value: Bucharest
  workflowTemplateRef:
    name: hello-template
    clusterScope: true
