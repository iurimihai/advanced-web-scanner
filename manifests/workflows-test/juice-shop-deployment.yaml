apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: juice-shop
  name: workflow-deployment
  namespace: workflows
spec:
  replicas: 1
  selector:
    matchLabels:
      app: juice-shop
  strategy: {}
  template:
    metadata:
  
      labels:
        app: juice-shop
    spec:
      containers:
      - image: iurimihai/kaniko-test
        name: nginx
        resources:
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: juice-shop
  name: juice-shop-service
  namespace: workflows
spec:
  ports:
  - port: 3001
    protocol: TCP
    targetPort: 3000
  selector:
    app: juice-shop
